# 交接版總整理

最後更新：2026-08-11

這份檔案是給下一位開發者直接接手用的。請先讀 `AGENTS.md`、`SPEC.md`、`PLAN.md`，再讀這份。

## 一句話先講

這個專案是給台灣高中職老師自己用的「時事命題網站」，目前已做到本機出題介面、題目編輯、列印、本機保存，雲端登入與雲端儲存功能已寫進程式，但本機驗證時卡在 Google 登入流程，還沒完整通過 M4。

## 交接時一定要守的工作規則

- 全程繁體中文、台灣用語，先講結論，再講理由。
- 每次只做一個里程碑，最多改 3 個檔案。
- 動工前一定要先講白話計畫、會改哪些檔案、可能壞什麼，然後等使用者回「可以開工嗎」或同意才動手。
- 沒有使用者明確同意前，不要安裝、不刪檔、不做會上雲端或會影響外部系統的動作。
- 做完一定要告訴使用者改了什麼、怎麼驗收，等使用者說「通過」後才做 commit。
- 開發與驗收先以本機為主，所有功能真的通過後，才規劃部署到 Vercel。

## 專案紅線

- 只能用純 HTML / CSS / JavaScript，不要加框架或套件。
- `index.html` 必須可以直接雙擊開啟。
- 介面文字、錯誤訊息、按鈕都要是繁體中文。
- 版面在電腦和手機都不能橫向溢位，列印要能成 A4。
- API 金鑰正式部署時只能放在 Vercel 環境變數，不可以寫進會進 git 的前端檔案。
- 不蒐集、不儲存、不傳送學生個資，只存老師本人的帳號與命題紀錄。

## 目前進度

### M0

- 已有題目骨架與本機頁面。
- 已有題目顯示區、列印區、基本畫面結構。

### M1

- 已有命題條件表單。
- 已有 8 個可編輯題格。
- 已有本機自動保存與兩顆列印鈕。

### M2

- 已做出「產生考題草稿」入口。
- 已串到本機出題流程的程式結構。
- 目前本機驗證還沒走到完整可用，因為登入和雲端流程還在卡點。

### M3 / M4

- `index.html` 已經寫入 Google 登入、登出、登入者名稱顯示、雲端命題紀錄清單、儲存、載入、另存、刪除的程式。
- `firestore.rules` 已經有「只能讀寫自己的 `users/{uid}/examSets`」規則。
- 但是在本機測試時，Google 登入沒有順利完成，所以雲端區塊沒有真正進來。

## 現在卡住的地方

- `http://localhost:3000/` 可以開。
- `http://127.0.0.1:3000/` 在當前測試裡會顯示無法連線。
- Google 登入流程曾出現兩種錯誤：
  - `auth/popup-closed-by-user`
  - `auth/unauthorized-domain`
- 目前頁面上的雲端區塊還沒有真正顯示出來，所以 M4 還不能算通過。

## 現有檔案與用途

- `index.html`：主頁面，包含本機保存、列印、Google 登入、雲端儲存區塊。
- `firestore.rules`：Firestore 安全規則。
- `firebase-config.js`：本機 Firebase 設定。
- `vendor/app-sdk.js`、`vendor/auth-sdk.js`、`vendor/firestore-sdk.js`：本機 Firebase 相容腳本。
- `api/generate.js`：AI 出題入口。
- `.gitignore`：已存在，會排除 `.env` 類檔案。

## 下一位開發者先做什麼

1. 先確認本機伺服器目前到底是用 `localhost` 還是 `127.0.0.1`。
2. 再確認 Firebase 專案後台是否把目前本機網域加進允許名單。
3. 先把 Google 登入跑通，讓登入者名稱與雲端區塊真的出現。
4. 登入成功後，再測雲端儲存、載入、另存、刪除。
5. 等 M4 完整通過，再往 M5 部署走。

## 驗收標準

- 本機頁面能正常打開。
- 點「使用 Google 登入」後，頁面要能顯示登入者名稱。
- 登入後，雲端區塊要出現。
- 按「儲存這份考卷」後，重新整理或換瀏覽器仍能看到紀錄。
- 「載入、另存、刪除」都要能成功。

## 補充提醒

- 如果你要改規則，先補進 `AGENTS.md`，讓後面每次都會記得。
- 如果你要改專案方向，以 `SPEC.md` 和 `PLAN.md` 為準，不要只看這份交接檔。
- 如果有安全性或金鑰相關調整，先確認不會把秘密寫進會進 git 的檔案。
